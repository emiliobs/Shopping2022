@model Shopping2022.Models.CityViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Crear Ciudad</h1>
<hr />
<div class="row">
    <div class="col-md-12">
        <form asp-action="AddCity">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
              <input  type="hidden" asp-for="StateId"/>

                <partial name="_City" />

            <div class="form-group m-2">
                <input type="submit" value="Crear" class="btn btn-outline-warning" />
                 <a asp-action="DetailsState" asp-route-id="@Model.StateId" class="btn btn-outline-dark">Regresar</a>
            </div>
        </form>
    </div>
</div>

<div>
   
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}